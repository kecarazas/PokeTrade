server:
  port: 8080

spring:
  application:
    name: PokeTrade
  datasource:
    url: jdbc:mysql://localhost:3306/PokeTrade
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
